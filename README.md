# My Portfolio

A personal portfolio built with React + Vite to showcase selected frontend and web app projects.

## How to Run

From the `creative-portfolio` folder:

```bash
npm install
npm run dev
```

Then open the local URL shown in the terminal (usually `http://localhost:5173`).

